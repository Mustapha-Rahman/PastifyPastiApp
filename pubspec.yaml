name: pastify
description: A social app for university students


publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.16.0 <3.0.0"


dependencies:
  flutter:
    sdk: flutter
  firebase_auth: ^3.3.7
  cloud_firestore: ^3.1.8
  firebase_storage: ^10.2.7
  firebase_core: ^1.12.0
  sizer: ^2.0.15
  google_fonts: ^2.3.1
  url_launcher: ^6.0.20
  flutter_launcher_icons: ^0.9.2






  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.2






dev_dependencies:
  flutter_test:
    sdk: flutter

flutter_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/images/logoo.png"

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^1.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true


  assets:

     - assets/images/back arrow.png
     - assets/images/tab home.png
     - assets/images/tab logout.png
     - assets/images/tab saved.png
     - assets/images/tab search.png
     - assets/images/umat champions league.jpg
     - assets/images/download.jfif
     - assets/images/whatsapp.jpg
     - assets/images/akwaabalogo.jpg
     - assets/images/season 1.jpg
     - assets/images/gmailgmail.png
     - assets/images/Deliveriess.png
     - assets/images/gamess.png
     - assets/images/logoo.png
     - assets/images/Pastify Newss.png
     - assets/images/vibess.png
     - assets/images/koking.jpg

